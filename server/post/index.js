// import Post from './post.model';
// import getLogger from '../lib/log';

// const log = getLogger(module);

// export const insertPosts = (dataDB) => {
//     Post.insertMany(dataDB, (err, posts) => {
//         if (err) {
//             log.error(`insert error -> ${ err }`);
//         }
    
//         log.info('insert posts -> ', posts);
//     });
// } 